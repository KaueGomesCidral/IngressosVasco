### Health (gateway)
GET http://localhost:8082/
Accept: application/json

### Criar reserva via Gateway (gateway pode precisar do JWT)
POST http://localhost:8082/api/tickets/reservations
Content-Type: application/json
Authorization: Bearer {{jwt_token}}

{
  "userId": "123e4567-e89b-12d3-a456-426614174000",
  "eventId": "event-001",
  "seatNumbers": ["A1"],
  "ttlSeconds": 300
}

### Observação:
# Se o gateway is configurado para o proxy ticket-service sob um caminho diferente, ajuste de acordo com o caminho.
